from django.conf.urls import url
from user import views
#联系人页面路由
urlpatterns = [
    # url(r'^user_profile/$', views.user_profile, name='user_profile'),
    # url(r'^user_properties/$', views.user_properties, name='user_properties')
]